diff --git a/examples/h007_clean_decode_gifstream.py b/examples/h007_clean_decode_gifstream.py
index 3d6af11eab5834e20ff63f097650d188fe2bdd16..b42468cd72b09234957abdb4effd88a2cfa8a4fa 100644
--- a/examples/h007_clean_decode_gifstream.py
+++ b/examples/h007_clean_decode_gifstream.py
@@ -3,8 +3,9 @@
 
 The decoder reads no source images, checkpoints, score files, or downstream
 outcomes outside the counted ZIP.  It reconstructs the entropy models from the
-counted configuration and ``nets.pt``, decodes splats, validates identities and
-camera sidecars, and writes a tensor-hash manifest.
+counted configuration and ``nets.pt``, restores exact canonical identities from
+the counted anchor-relative correction stream, validates camera sidecars, and
+writes a tensor-hash manifest.
 """
 
 from __future__ import annotations
@@ -239,15 +240,15 @@ def clean_decode(
     if config.get("official_commit") != OFFICIAL_COMMIT:
         raise ValueError("container official commit mismatch")
     if provenance_manifest is None or provenance_manifest_sha256 is None:
-        raise ValueError("all official/AP clean decodes require four-patch runtime provenance")
+        raise ValueError("all official/AP clean decodes require six-stage runtime provenance")
     repo_root = Path(__file__).resolve().parents[1]
     runtime_provenance = verify_runtime_provenance(
         provenance_manifest,
         repo_root,
         provenance_manifest_sha256,
     )
-    if len(runtime_provenance.get("patch_sha256", [])) != 5:
-        raise ValueError("clean decoder is not bound to the five-stage patch chain")
+    if len(runtime_provenance.get("patch_sha256", [])) != 6:
+        raise ValueError("clean decoder is not bound to the six-stage patch chain")
     counted_manifest = container / "preregistered_patch_chain_manifest.json"
     external_payload = provenance_manifest.resolve().read_bytes()
     if counted_manifest.read_bytes() != external_payload:
@@ -475,14 +476,22 @@ def clean_decode(
         int(config["GOP_size"]),
     ):
         raise ValueError("decoded temporal tensor shape disagrees with configuration")
-    sidecar_path = container / "ap_retained_canonical_ids.npy"
-    if sidecar_path.is_file():
-        sidecar_ids = np.load(sidecar_path, allow_pickle=False)
-        if sidecar_ids.dtype != np.int64:
-            raise ValueError("counted retained canonical IDs must use int64")
+    decoded_ids_path = None
+    decoded_ids_sha256 = None
+    if receipt is not None:
+        if codec.decoded_canonical_ids is None:
+            raise ValueError("AP decoder did not restore counted canonical identities")
+        decoded_ids = codec.decoded_canonical_ids.detach().cpu()
         _validate_counted_retained_identity(
-            torch.from_numpy(sidecar_ids), splats["anchors"].shape[0]
+            decoded_ids, splats["anchors"].shape[0]
+        )
+        decoded_ids_path = output_dir / "decoded_canonical_ids.npy"
+        np.save(
+            decoded_ids_path,
+            decoded_ids.numpy().astype(np.int64, copy=False),
+            allow_pickle=False,
         )
+        decoded_ids_sha256 = _sha256(decoded_ids_path)
 
     clean_render = counted_camera_render_benchmark(
         splats,
@@ -518,6 +527,10 @@ def clean_decode(
         "outcome_fields_read": [],
         "decoded_splats": decoded_path.name,
         "decoded_splats_sha256": _sha256(decoded_path),
+        "decoded_canonical_ids": (
+            decoded_ids_path.name if decoded_ids_path is not None else None
+        ),
+        "decoded_canonical_ids_sha256": decoded_ids_sha256,
         "camera_audit": camera_audit,
         "counted_model_audit": model_audit,
         "counted_camera_render": clean_render,
diff --git a/examples/simple_trainer_GIFStream.py b/examples/simple_trainer_GIFStream.py
index e1fce635dcdb0fb4e3b110b6c20d93818ee7ac80..2b260141ba0c3b96ecd3dedb6c9b1e555bb3d7e0 100644
--- a/examples/simple_trainer_GIFStream.py
+++ b/examples/simple_trainer_GIFStream.py
@@ -2448,7 +2448,7 @@ class Runner:
         self, step: int, ap_training_receipt_payload: Optional[bytes]
     ) -> Dict:
         if self.ap_runtime_provenance is None:
-            raise ValueError("official/AP producer lacks verified four-patch provenance")
+            raise ValueError("official/AP producer lacks verified six-stage provenance")
         if self.h007_training_receipt is None or self.h007_training_receipt_payload is None:
             raise ValueError("official/AP producer lacks its external frozen training receipt")
         training_config = self._producer_training_config()
@@ -2558,8 +2558,8 @@ class Runner:
         if self.ap_runtime_provenance is None:
             raise ValueError("official/AP compression lacks runtime provenance")
         patch_chain = list(self.ap_runtime_provenance["patch_sha256"])
-        if len(patch_chain) != 5:
-            raise ValueError("official/AP compression lacks a verified four-patch runtime chain")
+        if len(patch_chain) != 6:
+            raise ValueError("official/AP compression lacks a verified six-stage runtime chain")
         decoder_config = {
             "schema": DECODER_CONFIG_SCHEMA,
             "codec_family": "GIFStream",
diff --git a/gsplat/compression/ap_gifstream.py b/gsplat/compression/ap_gifstream.py
index 7350a345159112c07cdf0c6cc23ba4220cfd8240..db8111f97cf882b75133c24179abf5b9846ce9f6 100644
--- a/gsplat/compression/ap_gifstream.py
+++ b/gsplat/compression/ap_gifstream.py
@@ -24,7 +24,8 @@ from torch import Tensor
 
 
 AP_SCORE_SCHEMA = "h007.ap_scores.v2"
-AP_META_SCHEMA = "h007.ap_gifstream.codec.v4"
+AP_META_SCHEMA = "h007.ap_gifstream.codec.v5"
+AP_IDENTITY_CORRECTION_SCHEMA = "h007.ap_identity_corrections.v1"
 DETERMINISTIC_ZIP_EPOCH = (1980, 1, 1, 0, 0, 0)
 
 
@@ -731,6 +732,120 @@ def read_numpy_sidecar(compress_dir: str, meta: Mapping[str, Any]) -> np.ndarray
     return array
 
 
+def write_identity_corrections(
+    path: str, decoded_ids: np.ndarray, retained_ids: np.ndarray
+) -> Dict[str, Any]:
+    """Encode exact pre-quantization IDs as corrections to decoded anchors.
+
+    The anchor stream supplies the base ID for every real encoded row.  This
+    sidecar counts only a packed mismatch mask and one base-3 byte per changed
+    row.  Each axis correction is restricted to ``{-1, 0, 1}``; larger anchor
+    quantization drift is rejected instead of silently changing identity.
+    """
+
+    base = np.asarray(decoded_ids)
+    target = np.asarray(retained_ids)
+    if (
+        base.dtype != np.int64
+        or target.dtype != np.int64
+        or base.ndim != 2
+        or base.shape[1:] != (3,)
+        or target.shape != base.shape
+    ):
+        raise ValueError("identity-correction inputs must be matching int64 [N,3] arrays")
+    delta = target - base
+    mismatch = np.any(delta != 0, axis=1)
+    changed = delta[mismatch]
+    if changed.size and np.any((changed < -1) | (changed > 1)):
+        raise ValueError("decoded anchor identity drift exceeds one canonical voxel")
+    mask_payload = np.packbits(
+        mismatch.astype(np.uint8, copy=False), bitorder="little"
+    ).tobytes()
+    codes = (
+        (changed[:, 0] + 1) * 9
+        + (changed[:, 1] + 1) * 3
+        + (changed[:, 2] + 1)
+    ).astype(np.uint8, copy=False)
+    payload = mask_payload + codes.tobytes()
+    Path(path).write_bytes(payload)
+    return {
+        "schema": AP_IDENTITY_CORRECTION_SCHEMA,
+        "path": os.path.basename(path),
+        "row_count": int(base.shape[0]),
+        "mismatch_count": int(mismatch.sum()),
+        "mask_bytes": len(mask_payload),
+        "bytes": len(payload),
+        "sha256": hashlib.sha256(payload).hexdigest(),
+        "bitorder": "little",
+        "base": "round-decoded-anchor-div-voxel-size",
+        "code": "uint8-base3-dx-dy-dz-plus1",
+    }
+
+
+def read_identity_corrections(
+    compress_dir: str, meta: Mapping[str, Any], decoded_ids: np.ndarray
+) -> np.ndarray:
+    """Restore and validate exact retained identities from counted corrections."""
+
+    base = np.asarray(decoded_ids)
+    if base.dtype != np.int64 or base.ndim != 2 or base.shape[1:] != (3,):
+        raise ValueError("decoded identity base must be int64 [N,3]")
+    required = {
+        "schema",
+        "path",
+        "row_count",
+        "mismatch_count",
+        "mask_bytes",
+        "bytes",
+        "sha256",
+        "bitorder",
+        "base",
+        "code",
+    }
+    if set(meta) != required or meta.get("schema") != AP_IDENTITY_CORRECTION_SCHEMA:
+        raise ValueError("identity-correction metadata are malformed")
+    if (
+        meta.get("path") != "ap_identity_corrections.bin"
+        or meta.get("bitorder") != "little"
+        or meta.get("base") != "round-decoded-anchor-div-voxel-size"
+        or meta.get("code") != "uint8-base3-dx-dy-dz-plus1"
+        or type(meta.get("row_count")) is not int
+        or type(meta.get("mismatch_count")) is not int
+        or type(meta.get("mask_bytes")) is not int
+        or int(meta["row_count"]) != int(base.shape[0])
+        or int(meta["mismatch_count"]) < 0
+        or int(meta["mismatch_count"]) > int(base.shape[0])
+        or int(meta["mask_bytes"]) != (int(base.shape[0]) + 7) // 8
+    ):
+        raise ValueError("identity-correction dimensions/codec are invalid")
+    path = Path(compress_dir) / str(meta["path"])
+    payload = path.read_bytes()
+    if len(payload) != int(meta["bytes"]) or hashlib.sha256(payload).hexdigest() != str(
+        meta["sha256"]
+    ):
+        raise ValueError("identity-correction byte binding mismatch")
+    mask_extent = int(meta["mask_bytes"])
+    mask_tensor = unpack_bool_mask(payload[:mask_extent], int(base.shape[0]))
+    mismatch = mask_tensor.cpu().numpy().astype(np.bool_, copy=False)
+    if int(mismatch.sum()) != int(meta["mismatch_count"]):
+        raise ValueError("identity-correction mismatch count differs from packed mask")
+    codes = np.frombuffer(payload[mask_extent:], dtype=np.uint8)
+    if (
+        codes.shape != (int(meta["mismatch_count"]),)
+        or np.any(codes > 26)
+        or np.any(codes == 13)
+    ):
+        raise ValueError("identity-correction code extent/value is invalid")
+    restored = base.copy()
+    code64 = codes.astype(np.int64, copy=False)
+    restored[mismatch, 0] += code64 // 9 - 1
+    restored[mismatch, 1] += (code64 // 3) % 3 - 1
+    restored[mismatch, 2] += code64 % 3 - 1
+    if np.unique(restored, axis=0).shape[0] != restored.shape[0]:
+        raise ValueError("restored retained canonical IDs are not unique")
+    return restored
+
+
 def recompute_sorted_codec_invariants(
     *,
     official_retain_mask: Tensor,
@@ -861,7 +976,7 @@ def recompute_sorted_codec_invariants(
         "official_estimated_time_bytes": official_mass,
         "ap_estimated_time_bytes": ap_mass,
         "plas_permutation_complete": True,
-        "id_order_definition": "retained_sidecar_rows_equal_prequantization_encoded_rows_at_real_mask",
+        "id_order_definition": "counted_corrections_restore_prequantization_ids_in_encoded_real_row_order",
     }
 
 
diff --git a/gsplat/compression/gifstream_end2end_compression.py b/gsplat/compression/gifstream_end2end_compression.py
index 80df237c137b07a9577c629cd4f608cd6f515049..df5fdd9e573007cbd60b2a84d9a61154e363c18e 100644
--- a/gsplat/compression/gifstream_end2end_compression.py
+++ b/gsplat/compression/gifstream_end2end_compression.py
@@ -19,13 +19,13 @@ from gsplat.compression.ap_gifstream import (
     canonical_voxel_ids,
     load_aligned_score_artifact,
     read_ap_mask,
-    read_numpy_sidecar,
+    read_identity_corrections,
     recompute_sorted_codec_invariants,
     tensor_mapping_sha256,
     variant_metadata,
     variant_spec,
     write_ap_mask,
-    write_numpy_sidecar,
+    write_identity_corrections,
 )
 from gsplat.compression.h007_runtime_provenance import verify_runtime_provenance
 from gsplat.utils import inverse_log_transform, log_transform
@@ -113,6 +113,7 @@ class GIFStreamEnd2endCompression:
     use_sort: bool = True
     verbose: bool = True
     ap_config: Optional[Dict[str, Any]] = None
+    decoded_canonical_ids: Optional[Tensor] = None
 
     def _get_compress_fn(self, param_name: str) -> Callable:
         compress_fn_map = {
@@ -427,15 +428,33 @@ class GIFStreamEnd2endCompression:
                 os.path.join(compress_dir, "ap_active_row_mask.bin"),
                 real_mask & (splats["factors"][:, 0] > 0),
             )
-            retained_ids_path = os.path.join(compress_dir, "ap_retained_canonical_ids.npy")
-            retained_ids_meta = write_numpy_sidecar(
-                retained_ids_path,
+            decoded_anchor_rows = _decompress_png_16bit(
+                compress_dir, "anchors", meta["anchors"]
+            )
+            decoded_ids = (
+                torch.round(
+                    decoded_anchor_rows[real_mask.detach().cpu()] / float(voxel_size)
+                )
+                .to(torch.int64)
+                .numpy()
+                .astype(np.int64, copy=False)
+            )
+            retained_ids = (
                 canonical_ids[real_mask]
                 .detach()
                 .cpu()
                 .numpy()
-                .astype(np.int64, copy=False),
+                .astype(np.int64, copy=False)
             )
+            identity_path = os.path.join(compress_dir, "ap_identity_corrections.bin")
+            identity_meta = write_identity_corrections(
+                identity_path, decoded_ids, retained_ids
+            )
+            if not np.array_equal(
+                read_identity_corrections(compress_dir, identity_meta, decoded_ids),
+                retained_ids,
+            ):
+                raise AssertionError("counted identity corrections failed exact encoder replay")
             meta["__ap__"] = {
                 "schema": AP_META_SCHEMA,
                 "variant": variant_metadata(ap_variant),
@@ -449,7 +468,7 @@ class GIFStreamEnd2endCompression:
                 "real_row_mask": real_mask_meta,
                 "padding_row_mask": padding_mask_meta,
                 "active_row_mask": active_mask_meta,
-                "retained_canonical_ids": retained_ids_meta,
+                "identity_corrections": identity_meta,
             }
 
         with open(os.path.join(compress_dir, "meta.json"), "w") as f:
@@ -467,13 +486,14 @@ class GIFStreamEnd2endCompression:
         def inverse_sigmoid(x):
             return -torch.log(1/(x.clamp(1e-7,1-1e-7)) - 1)
         
+        self.decoded_canonical_ids = None
         with open(os.path.join(compress_dir, "meta.json"), "r") as f:
             meta = json.load(f)
 
         ap_meta = meta.get("__ap__")
         if ap_meta is not None and ap_meta.get("schema") != AP_META_SCHEMA:
             raise ValueError("unsupported AP-GIFStream metadata schema")
-        ap_sidecars = ap_masks = None
+        ap_masks = None
         if ap_meta is not None:
             ap_cfg = dict(self.ap_config or {})
             provenance_required = {
@@ -500,19 +520,6 @@ class GIFStreamEnd2endCompression:
                 != ap_meta["runtime_provenance"]["patch_sha256"]
             ):
                 raise ValueError("counted AP score receipt/runtime provenance mismatch")
-            ap_sidecars = {
-                "retained_canonical_ids": read_numpy_sidecar(
-                    compress_dir, ap_meta["retained_canonical_ids"]
-                )
-            }
-            retained_ids = ap_sidecars["retained_canonical_ids"]
-            if (
-                retained_ids.dtype != np.int64
-                or retained_ids.ndim != 2
-                or retained_ids.shape[1] != 3
-                or np.unique(retained_ids, axis=0).shape[0] != retained_ids.shape[0]
-            ):
-                raise ValueError("recorded retained canonical IDs are malformed/duplicated")
             ap_masks = {
                 name: read_ap_mask(compress_dir, ap_meta[name], device=device)
                 for name in (
@@ -609,10 +616,19 @@ class GIFStreamEnd2endCompression:
         splats["anchors"] = torch.round(splats["anchors"]/voxel_size)*voxel_size
 
         if ap_meta is not None:
+            decoded_ids = (
+                torch.round(splats["anchors"].detach().cpu() / float(voxel_size))
+                .to(torch.int64)
+                .numpy()
+                .astype(np.int64, copy=False)
+            )
             retained_ids = torch.from_numpy(
-                ap_sidecars["retained_canonical_ids"].copy()
+                read_identity_corrections(
+                    compress_dir, ap_meta["identity_corrections"], decoded_ids
+                )
             ).to(device=device, dtype=torch.int64)
             _validate_counted_retained_identity(retained_ids, len(splats["anchors"]))
+            self.decoded_canonical_ids = retained_ids.detach().clone()
 
         #* recover time features
         choose_idx = splats["factors"][:,0] > 0
diff --git a/gsplat/compression/h007_certification.py b/gsplat/compression/h007_certification.py
index 72c4f22a1a63d543d38f43321399e0dded950923..a8308ae68f0edde8a178441e8dd5543cb7f15597 100644
--- a/gsplat/compression/h007_certification.py
+++ b/gsplat/compression/h007_certification.py
@@ -3,7 +3,7 @@
 The Stage-02 marker is not a generic directory checksum.  It is an exclusive,
 no-follow freeze of an exact five-scene x five-GOP reference grid, the selected
 real nested sequence archives, ordinary image/rate preconditions, and the
-active four-patch evaluator provenance.  Validation reopens and recomputes the
+active six-stage evaluator provenance.  Validation reopens and recomputes the
 complete closure; a caller-supplied PASS object is never trusted.
 """
 
@@ -273,8 +273,8 @@ def _verify_runtime(
         repo_root,
         require_sha256(provenance_manifest_sha256, "patch-chain manifest SHA-256"),
     )
-    if len(receipt.get("patch_sha256", [])) != 5:
-        raise ValueError("Stage 02 requires the exact five-stage runtime provenance")
+    if len(receipt.get("patch_sha256", [])) != 6:
+        raise ValueError("Stage 02 requires the exact six-stage runtime provenance")
     return receipt
 
 
@@ -1006,7 +1006,7 @@ def validate_stage02_freeze(
         read_regular_bytes(root / RUNTIME_RECEIPT_NAME) != runtime_payload
         or sha256_bytes(runtime_payload) != freeze["runtime_provenance_sha256"]
     ):
-        raise ValueError("Stage-02 four-patch runtime receipt mismatch")
+        raise ValueError("Stage-02 six-stage runtime receipt mismatch")
     reference_cases, reference_files, reference_rebuilds = _validate_reference_grid(
         root, evaluator_sha, repo_root
     )
diff --git a/gsplat/compression/h007_runtime_provenance.py b/gsplat/compression/h007_runtime_provenance.py
index 9391bfe9dc7279a1a423ff018dc2f4bca18f143c..5302b4a8d65fccdb2fa7d3d23313309696f5e040 100644
--- a/gsplat/compression/h007_runtime_provenance.py
+++ b/gsplat/compression/h007_runtime_provenance.py
@@ -1,7 +1,7 @@
 """Fail-closed runtime provenance for the H007 AP-GIFStream experiment.
 
 The preregistration manifest deliberately lives outside the patched GIFStream
-tree.  It binds the five patch payloads and a normalized post-apply source-tree
+tree.  It binds the six patch payloads and a normalized post-apply source-tree
 digest without making a source file contain its own hash.
 """
 
@@ -20,7 +20,7 @@ OFFICIAL_COMMIT = "c98486632e7dafd830740b1a1692bd08c48b96e3"
 PATCH1_SHA256 = "65ecd8f1fe30fac5d5bce3aed295cfe04f0b0582f106efaf37489b005d2e431b"
 PATCH2_SHA256 = "347e3fbb59cdab49160a0c5291ecbd375e2de37d6bf3dea3e927d07f0ed8a253"
 PATCH2B_SHA256 = "537b7a6c1607204000f6d4ce198d60c0b69917e46cdd89e9d6fcc431d37af40b"
-PATCH_STAGES = ("patch1", "patch2", "patch2b", "patch3", "patch4")
+PATCH_STAGES = ("patch1", "patch2", "patch2b", "patch3", "patch4", "patch5")
 TREE_ROOTS = ("examples", "gsplat", "third_party")
 TREE_ROOT_FILES: Tuple[str, ...] = ("setup.py",)
 TREE_SUFFIXES = (".c", ".cc", ".cpp", ".cu", ".cuh", ".h", ".hpp", ".py")
@@ -193,7 +193,7 @@ def provenance_manifest_template(
 
     patch_paths_and_hashes = list(patch_paths_and_hashes)
     if len(patch_paths_and_hashes) != len(PATCH_STAGES):
-        raise ValueError("manifest template requires exactly five patch payloads")
+        raise ValueError("manifest template requires exactly six patch payloads")
     rows = []
     for stage, (path, digest) in zip(PATCH_STAGES, patch_paths_and_hashes):
         rows.append(
diff --git a/gsplat/compression/h007_sequence_container.py b/gsplat/compression/h007_sequence_container.py
index 1ce7dea02e3e9fcbac052dd69b902ef6fa401807..b584cb4e4f6c262cfd1a95e02502c9fb5fcb3b22 100644
--- a/gsplat/compression/h007_sequence_container.py
+++ b/gsplat/compression/h007_sequence_container.py
@@ -1247,6 +1247,51 @@ def _packed_bool_mask_contract(
         raise ValueError(f"{label} packed-mask true count differs from its bytes")
 
 
+def _identity_correction_payload_contract(
+    payload: bytes, record: Mapping[str, Any], label: str
+) -> None:
+    required = {
+        "schema",
+        "path",
+        "row_count",
+        "mismatch_count",
+        "mask_bytes",
+        "bytes",
+        "sha256",
+        "bitorder",
+        "base",
+        "code",
+    }
+    if (
+        not isinstance(record, dict)
+        or set(record) != required
+        or record.get("schema") != "h007.ap_identity_corrections.v1"
+        or record.get("path") != "ap_identity_corrections.bin"
+        or record.get("bitorder") != "little"
+        or record.get("base") != "round-decoded-anchor-div-voxel-size"
+        or record.get("code") != "uint8-base3-dx-dy-dz-plus1"
+    ):
+        raise ValueError(f"{label} metadata are malformed")
+    rows = _positive_int(record.get("row_count"), f"{label} row count")
+    mismatches = _nonnegative_int(
+        record.get("mismatch_count"), f"{label} mismatch count"
+    )
+    mask_bytes = _positive_int(record.get("mask_bytes"), f"{label} mask bytes")
+    if (
+        mismatches > rows
+        or mask_bytes != (rows + 7) // 8
+        or record.get("bytes") != len(payload)
+        or len(payload) != mask_bytes + mismatches
+        or sha256_bytes(payload) != record.get("sha256")
+    ):
+        raise ValueError(f"{label} byte extent/binding is invalid")
+    _packed_bool_mask_contract(
+        payload[:mask_bytes], rows, mismatches, f"{label} mismatch mask"
+    )
+    if any(value > 26 or value == 13 for value in payload[mask_bytes:]):
+        raise ValueError(f"{label} contains an invalid base-3 correction code")
+
+
 def _canonical_torch_zip_bytes(root: str, members: Mapping[str, bytes]) -> bytes:
     fixed_prefix = [
         f"{root}/data.pkl",
@@ -2273,7 +2318,7 @@ def _validate_runtime_provenance_shape(value: Any, label: str) -> Dict[str, Any]
         value["schema"] != "h007.ap_gifstream.runtime_provenance.v1"
         or value["official_commit"] != OFFICIAL_COMMIT
         or not isinstance(value["patch_sha256"], list)
-        or len(value["patch_sha256"]) != 5
+        or len(value["patch_sha256"]) != 6
         or not isinstance(tree, dict)
         or set(tree) != tree_required
         or tree["schema"] != "h007.normalized_code_tree.v1"
@@ -2481,12 +2526,12 @@ def _required_gifstream_streams(meta: Mapping[str, Any], method: str) -> Dict[st
             "real_row_mask",
             "padding_row_mask",
             "active_row_mask",
-            "retained_canonical_ids",
+            "identity_corrections",
         }
         if (
             not isinstance(ap_meta, dict)
             or set(ap_meta) != ap_required
-            or ap_meta.get("schema") != "h007.ap_gifstream.codec.v4"
+            or ap_meta.get("schema") != "h007.ap_gifstream.codec.v5"
             or ap_meta.get("variant", {}).get("name") != method
         ):
             raise ValueError("AP codec metadata variant differs from decoder config")
@@ -2613,7 +2658,7 @@ def _required_gifstream_streams(meta: Mapping[str, Any], method: str) -> Dict[st
             != allocation["ap_estimated_time_bytes"]
             or allocation["plas_permutation_complete"] is not True
             or allocation["id_order_definition"]
-            != "retained_sidecar_rows_equal_prequantization_encoded_rows_at_real_mask"
+            != "counted_corrections_restore_prequantization_ids_in_encoded_real_row_order"
         ):
             raise ValueError("AP allocation categorical metadata are invalid")
         if (
@@ -2631,9 +2676,9 @@ def _required_gifstream_streams(meta: Mapping[str, Any], method: str) -> Dict[st
             "real_row_mask": ("ap_real_row_mask.bin", "ap_real_row_mask"),
             "padding_row_mask": ("ap_padding_row_mask.bin", "ap_padding_row_mask"),
             "active_row_mask": ("ap_active_row_mask.bin", "ap_active_row_mask"),
-            "retained_canonical_ids": (
-                "ap_retained_canonical_ids.npy",
-                "ap_retained_canonical_ids",
+            "identity_corrections": (
+                "ap_identity_corrections.bin",
+                "ap_identity_corrections",
             ),
         }
         for key, (filename, role) in fixed_ap.items():
@@ -2656,15 +2701,34 @@ def _required_gifstream_streams(meta: Mapping[str, Any], method: str) -> Dict[st
                 ):
                     raise ValueError(f"AP mask metadata is malformed: {key}")
             else:
-                expected_numpy_shape = [real_count, 3]
+                required = {
+                    "schema",
+                    "path",
+                    "row_count",
+                    "mismatch_count",
+                    "mask_bytes",
+                    "bytes",
+                    "sha256",
+                    "bitorder",
+                    "base",
+                    "code",
+                }
                 if (
-                    set(record) != {"path", "shape", "dtype", "bytes", "sha256"}
-                    or record.get("dtype") != "<i8"
-                    or not isinstance(record.get("shape"), list)
-                    or any(type(value) is not int for value in record["shape"])
-                    or record.get("shape") != expected_numpy_shape
+                    set(record) != required
+                    or record.get("schema") != "h007.ap_identity_corrections.v1"
+                    or record.get("row_count") != real_count
+                    or type(record.get("mismatch_count")) is not int
+                    or record.get("mismatch_count") < 0
+                    or record.get("mismatch_count") > real_count
+                    or record.get("mask_bytes") != (real_count + 7) // 8
+                    or record.get("bytes")
+                    != record.get("mask_bytes") + record.get("mismatch_count")
+                    or record.get("bitorder") != "little"
+                    or record.get("base")
+                    != "round-decoded-anchor-div-voxel-size"
+                    or record.get("code") != "uint8-base3-dx-dy-dz-plus1"
                 ):
-                    raise ValueError(f"AP NumPy-sidecar metadata is malformed: {key}")
+                    raise ValueError(f"AP identity-correction metadata is malformed: {key}")
             _nonnegative_int(record.get("bytes"), f"AP sidecar {key} byte count")
             _require_sha256(record.get("sha256"), f"AP sidecar {key} SHA-256")
             streams[filename] = role
@@ -2757,8 +2821,8 @@ def _validate_gifstream_decoder_closure(
         raise ValueError("GOP decoder config is not the frozen GIFStream decoder contract")
     _validate_decoder_config_discrete_types(config)
     patch_chain = config.get("patch_chain_sha256")
-    if not isinstance(patch_chain, list) or len(patch_chain) != 5:
-        raise ValueError("GIFStream GOP is not bound to the four-patch runtime")
+    if not isinstance(patch_chain, list) or len(patch_chain) != 6:
+        raise ValueError("GIFStream GOP is not bound to the six-stage runtime")
     for index, digest in enumerate(patch_chain):
         _require_sha256(digest, f"patch-chain entry {index} SHA-256")
     manifest_sha = _require_sha256(
@@ -2799,7 +2863,7 @@ def _validate_gifstream_decoder_closure(
         or preregistered_value["official_commit"] != OFFICIAL_COMMIT
         or not isinstance(prereg_patches, list)
         or [row.get("stage") for row in prereg_patches]
-        != ["patch1", "patch2", "patch2b", "patch3", "patch4"]
+        != ["patch1", "patch2", "patch2b", "patch3", "patch4", "patch5"]
         or any(
             not isinstance(row, dict)
             or set(row) != {"stage", "path", "sha256"}
@@ -2829,7 +2893,7 @@ def _validate_gifstream_decoder_closure(
             "real_row_mask",
             "padding_row_mask",
             "active_row_mask",
-            "retained_canonical_ids",
+            "identity_corrections",
         ):
             record = ap_meta[key]
             sidecar_payload = handle.read(record["path"])
@@ -2845,6 +2909,10 @@ def _validate_gifstream_decoder_closure(
                     record["true_count"],
                     f"AP {key}",
                 )
+            else:
+                _identity_correction_payload_contract(
+                    sidecar_payload, record, "AP identity corrections"
+                )
     payload_manifest_payload = handle.read("gifstream_payload_manifest.json")
     payload_manifest = _strict_canonical_json(
         payload_manifest_payload, "counted GIFStream payload manifest"
@@ -2936,20 +3004,11 @@ def _validate_gifstream_decoder_closure(
             else:
                 raise ValueError(f"unrouted counted NumPy ZIP role: {name}")
         elif name.endswith(".npy"):
-            ap_numpy_key = {
-                "ap_retained_canonical_ids.npy": "retained_canonical_ids",
-            }.get(name)
-            if ap_numpy_key is None:
-                _npy_payload_contract(payload, name)
-            else:
-                allocation = meta["__ap__"]["allocation"]
-                expected_shape = [int(allocation["real_row_count"]), 3]
-                _ap_numpy_sidecar_contract(
-                    payload,
-                    name,
-                    meta["__ap__"][ap_numpy_key],
-                    expected_shape,
-                )
+            _npy_payload_contract(payload, name)
+        elif name == "ap_identity_corrections.bin":
+            _identity_correction_payload_contract(
+                payload, meta["__ap__"]["identity_corrections"], name
+            )
         elif name.endswith(".bin") and "mask" not in role:
             _entropy_stream_contract(payload, name)
     nets_audit = _torch_save_zip_contract(
@@ -4291,7 +4350,7 @@ def validate_eligibility_recomputation_contract(
         runtime["schema"] != "h007.ap_gifstream.runtime_provenance.v1"
         or runtime["official_commit"] != OFFICIAL_COMMIT
         or not isinstance(patches, list)
-        or len(patches) != 5
+        or len(patches) != 6
         or not isinstance(tree, dict)
         or set(tree)
         != {
@@ -4788,7 +4847,7 @@ def _validate_ordinary_rate_quality_evidence(
 
     The request JSON contains no trusted PASS flag.  This function opens every
     referenced archive and every source RGB member, validates the active
-    four-patch runtime, and recomputes the ordinary rate/quality accounting.
+    six-stage runtime, and recomputes the ordinary rate/quality accounting.
     """
 
     evidence = _strict_canonical_json(payload, "ordinary rate-quality evidence")
@@ -4857,8 +4916,8 @@ def _validate_ordinary_rate_quality_evidence(
         runtime_repo_root,
         runtime_sha,
     )
-    if len(runtime_receipt.get("patch_sha256", [])) != 5:
-        raise ValueError("ordinary evidence does not bind the exact four-patch runtime")
+    if len(runtime_receipt.get("patch_sha256", [])) != 6:
+        raise ValueError("ordinary evidence does not bind the exact six-stage runtime")
     evaluator_relative = _nonempty_string(
         evidence["evaluator_relative_path"], "ordinary evaluator relative path"
     )
diff --git a/tests/test_ap_gifstream_core.py b/tests/test_ap_gifstream_core.py
index 701adca448a03f0a41712db61be25a6f36c41598..0f02c90b54f735658938c589649450efafab3108 100644
--- a/tests/test_ap_gifstream_core.py
+++ b/tests/test_ap_gifstream_core.py
@@ -16,9 +16,11 @@ from gsplat.compression.ap_gifstream import (
     deterministic_zip_directory,
     load_aligned_score_artifact,
     pack_bool_mask,
+    read_identity_corrections,
     recompute_sorted_codec_invariants,
     tensor_mapping_sha256,
     unpack_bool_mask,
+    write_identity_corrections,
 )
 from gsplat.compression.h007_clean_runtime import (
     build_decoder_modules,
@@ -51,7 +53,14 @@ class APGIFStreamCoreTest(unittest.TestCase):
             "schema": "h007.ap_gifstream.runtime_provenance.v1",
             "manifest_sha256": "1" * 64,
             "official_commit": provenance.OFFICIAL_COMMIT,
-            "patch_sha256": ["2" * 64, "3" * 64, "4" * 64, "6" * 64, "7" * 64],
+            "patch_sha256": [
+                "2" * 64,
+                "3" * 64,
+                "4" * 64,
+                "6" * 64,
+                "7" * 64,
+                "8" * 64,
+            ],
             "normalized_code_tree": {"sha256": "5" * 64},
         }
 
@@ -148,7 +157,14 @@ class APGIFStreamCoreTest(unittest.TestCase):
                 runtime_manifest_sha256=np.asarray("1" * 64),
                 normalized_code_tree_sha256=np.asarray("5" * 64),
                 patch_chain_sha256=np.asarray(
-                    ["2" * 64, "3" * 64, "4" * 64, "6" * 64]
+                    [
+                        "2" * 64,
+                        "3" * 64,
+                        "4" * 64,
+                        "6" * 64,
+                        "7" * 64,
+                        "8" * 64,
+                    ]
                 ),
             )
             score, eligible, estimated_bytes, allocation, audit = load_aligned_score_artifact(
@@ -191,6 +207,36 @@ class APGIFStreamCoreTest(unittest.TestCase):
         restored = unpack_bool_mask(payload, mask.numel())
         self.assertTrue(torch.equal(mask, restored))
 
+    def test_identity_corrections_restore_unique_prequantization_ids(self):
+        decoded = np.asarray(
+            [[0, 0, 0], [0, 0, 0], [2, 1, 0], [4, 0, 0]], dtype=np.int64
+        )
+        retained = np.asarray(
+            [[0, 0, 0], [1, 0, 0], [2, 0, 0], [4, 0, 1]], dtype=np.int64
+        )
+        with tempfile.TemporaryDirectory() as tmp:
+            path = Path(tmp) / "ap_identity_corrections.bin"
+            meta = write_identity_corrections(str(path), decoded, retained)
+            restored = read_identity_corrections(tmp, meta, decoded)
+            np.testing.assert_array_equal(restored, retained)
+            self.assertEqual(meta["row_count"], 4)
+            self.assertEqual(meta["mismatch_count"], 3)
+            self.assertEqual(meta["bytes"], 4)
+            tampered = bytearray(path.read_bytes())
+            tampered[-1] = 27
+            path.write_bytes(tampered)
+            with self.assertRaisesRegex(ValueError, "binding|code"):
+                read_identity_corrections(tmp, meta, decoded)
+
+    def test_identity_corrections_reject_more_than_one_voxel_drift(self):
+        with tempfile.TemporaryDirectory() as tmp:
+            with self.assertRaisesRegex(ValueError, "exceeds one"):
+                write_identity_corrections(
+                    str(Path(tmp) / "ap_identity_corrections.bin"),
+                    np.asarray([[0, 0, 0]], dtype=np.int64),
+                    np.asarray([[2, 0, 0]], dtype=np.int64),
+                )
+
     def test_tensor_mapping_hash_is_order_invariant(self):
         left = {
             "b": torch.tensor([2.0]),
diff --git a/tests/test_h007_certification_stdlib.py b/tests/test_h007_certification_stdlib.py
index 13f2afdcb9ed69d24a90238e3c47eb7a87122fc0..f24aee185da39023b04341ec339fdeffaff67e5f 100644
--- a/tests/test_h007_certification_stdlib.py
+++ b/tests/test_h007_certification_stdlib.py
@@ -135,7 +135,14 @@ class H007CertificationStdlibTest(unittest.TestCase):
                     "schema": "h007.ap_gifstream.runtime_provenance.v1",
                     "manifest_sha256": "1" * 64,
                     "official_commit": "c98486632e7dafd830740b1a1692bd08c48b96e3",
-                    "patch_sha256": ["2" * 64, "3" * 64, "4" * 64, "5" * 64, "6" * 64],
+                    "patch_sha256": [
+                        "2" * 64,
+                        "3" * 64,
+                        "4" * 64,
+                        "5" * 64,
+                        "6" * 64,
+                        "7" * 64,
+                    ],
                     "normalized_code_tree": {
                         "schema": "h007.normalized_code_tree.v1",
                         "normalization": "sorted-posix-path+lf-bytes+uint64le-lengths",
@@ -341,7 +348,7 @@ class H007CertificationStdlibTest(unittest.TestCase):
     def test_manual_case_without_generation_closure_is_rejected(self):
         stage = {
             "freeze": {"evaluator_sha256": "1" * 64},
-            "runtime_provenance": {"patch_sha256": ["a" * 64] * 5},
+            "runtime_provenance": {"patch_sha256": ["a" * 64] * 6},
         }
         sequence = {
             "archive_sha256": "2" * 64,
diff --git a/tests/test_h007_runtime_provenance_stdlib.py b/tests/test_h007_runtime_provenance_stdlib.py
index 1065ea4a2aaa51517843b3ab14fe9946623dcb6c..69dae212c9438fcaa2ef1ae009cec132495f7e5e 100644
--- a/tests/test_h007_runtime_provenance_stdlib.py
+++ b/tests/test_h007_runtime_provenance_stdlib.py
@@ -1,4 +1,4 @@
-"""Stdlib-only tamper tests for the five-stage H007 provenance contract."""
+"""Stdlib-only tamper tests for the six-stage H007 provenance contract."""
 
 from __future__ import annotations
 
@@ -38,7 +38,7 @@ class H007RuntimeProvenanceStdlibTest(unittest.TestCase):
         (self.repo / "examples" / "fixture.py").write_text("VALUE = 1\n", encoding="utf-8")
         patch_dir = self.root / "patches"
         patch_dir.mkdir()
-        self.patch_payloads = [f"patch-{index}\n".encode() for index in range(1, 6)]
+        self.patch_payloads = [f"patch-{index}\n".encode() for index in range(1, 7)]
         self.patch_hashes = []
         rows = []
         for stage, payload in zip(provenance.PATCH_STAGES, self.patch_payloads):
@@ -77,7 +77,7 @@ class H007RuntimeProvenanceStdlibTest(unittest.TestCase):
                 self.manifest, self.repo, self.manifest_sha
             )
 
-    def test_clean_five_stage_chain_is_accepted(self) -> None:
+    def test_clean_six_stage_chain_is_accepted(self) -> None:
         receipt = self._verify()
         self.assertEqual(receipt["patch_sha256"], self.patch_hashes)
         self.assertEqual(receipt["normalized_code_tree"]["file_count"], 2)
diff --git a/tests/test_h007_sequence_container.py b/tests/test_h007_sequence_container.py
index 79bb32e96bafd5faa7820276f97a130cd5dea3b9..90f9dccdb6335ef9c863797cf911b9ba122bd2c3 100644
--- a/tests/test_h007_sequence_container.py
+++ b/tests/test_h007_sequence_container.py
@@ -143,7 +143,7 @@ def fixture_ap_meta(method="ap-gifstream-full"):
         "time_entropy_model_frozen_after_freeze": True,
         "runtime_manifest_sha256": digest,
         "normalized_code_tree_sha256": digest,
-        "patch_chain_sha256": [digest] * 5,
+        "patch_chain_sha256": [digest] * 6,
         "anchor_count": anchor_count,
         "eligible_count": 3,
     }
@@ -163,13 +163,13 @@ def fixture_ap_meta(method="ap-gifstream-full"):
         "official_estimated_time_bytes": 100,
         "ap_estimated_time_bytes": 100,
         "plas_permutation_complete": True,
-        "id_order_definition": "retained_sidecar_rows_equal_prequantization_encoded_rows_at_real_mask",
+        "id_order_definition": "counted_corrections_restore_prequantization_ids_in_encoded_real_row_order",
     }
     runtime = {
         "schema": "h007.ap_gifstream.runtime_provenance.v1",
         "manifest_sha256": digest,
         "official_commit": sequence.OFFICIAL_COMMIT,
-        "patch_sha256": [digest] * 5,
+        "patch_sha256": [digest] * 6,
         "normalized_code_tree": {
             "schema": "h007.normalized_code_tree.v1",
             "normalization": "fixture",
@@ -189,9 +189,20 @@ def fixture_ap_meta(method="ap-gifstream-full"):
         "sha256": digest,
         "bitorder": "little",
     }
-    numpy = {"path": "", "shape": [], "dtype": "<i8", "bytes": 1, "sha256": digest}
+    corrections = {
+        "schema": "h007.ap_identity_corrections.v1",
+        "path": "ap_identity_corrections.bin",
+        "row_count": real_count,
+        "mismatch_count": 0,
+        "mask_bytes": (real_count + 7) // 8,
+        "bytes": (real_count + 7) // 8,
+        "sha256": digest,
+        "bitorder": "little",
+        "base": "round-decoded-anchor-div-voxel-size",
+        "code": "uint8-base3-dx-dy-dz-plus1",
+    }
     core["__ap__"] = {
-        "schema": "h007.ap_gifstream.codec.v4",
+        "schema": "h007.ap_gifstream.codec.v5",
         "variant": dict(sequence.AP_VARIANT_METADATA[method]),
         "score": score,
         "allocation": allocation,
@@ -207,11 +218,7 @@ def fixture_ap_meta(method="ap-gifstream-full"):
             "true_count": anchor_count - real_count,
         },
         "active_row_mask": {**mask, "path": "ap_active_row_mask.bin", "true_count": active_count},
-        "retained_canonical_ids": {
-            **numpy,
-            "path": "ap_retained_canonical_ids.npy",
-            "shape": [real_count, 3],
-        },
+        "identity_corrections": corrections,
     }
     return core
 
@@ -796,7 +803,7 @@ class H007SequenceContainerTest(unittest.TestCase):
             "schema": "h007.ap_gifstream.runtime_provenance.v1",
             "manifest_sha256": hashlib.sha256(runtime_manifest.read_bytes()).hexdigest(),
             "official_commit": sequence.OFFICIAL_COMMIT,
-            "patch_sha256": ["a" * 64] * 5,
+            "patch_sha256": ["a" * 64] * 6,
             "normalized_code_tree": {
                 "schema": "h007.normalized_code_tree.v1",
                 "normalization": "sorted-posix-path+lf-bytes+uint64le-lengths",
@@ -923,7 +930,7 @@ class H007SequenceContainerTest(unittest.TestCase):
             "file_count": 1,
             "sha256": "a" * 64,
         }
-        patch_hashes = [f"{index:x}" * 64 for index in range(1, 6)]
+        patch_hashes = [f"{index:x}" * 64 for index in range(1, 7)]
         runtime_manifest_payload = sequence.canonical_json_bytes(
             {
                 "schema": "h007.ap_gifstream.patch_chain_manifest.v1",
@@ -935,7 +942,8 @@ class H007SequenceContainerTest(unittest.TestCase):
                         "sha256": digest,
                     }
                     for stage, digest in zip(
-                        ("patch1", "patch2", "patch2b", "patch3", "patch4"), patch_hashes
+                        ("patch1", "patch2", "patch2b", "patch3", "patch4", "patch5"),
+                        patch_hashes,
                     )
                 ],
                 "normalized_code_tree": normalized_tree,
@@ -1873,40 +1881,28 @@ class H007SequenceContainerTest(unittest.TestCase):
                 wider, "score.npz", schema, order
             )
 
-    def test_ap_numpy_sidecars_bind_exact_int64_row_contracts(self):
+    def test_ap_identity_corrections_bind_exact_mask_and_codes(self):
+        payload = bytes([0b00000101, 14, 12])
         record = {
-            "path": "ap_retained_canonical_ids.npy",
-            "shape": [3, 3],
-            "dtype": "<i8",
-            "bytes": 1,
-            "sha256": "a" * 64,
+            "schema": "h007.ap_identity_corrections.v1",
+            "path": "ap_identity_corrections.bin",
+            "row_count": 3,
+            "mismatch_count": 2,
+            "mask_bytes": 1,
+            "bytes": len(payload),
+            "sha256": hashlib.sha256(payload).hexdigest(),
+            "bitorder": "little",
+            "base": "round-decoded-anchor-div-voxel-size",
+            "code": "uint8-base3-dx-dy-dz-plus1",
         }
-        sequence._ap_numpy_sidecar_contract(
-            fixture_npy((3, 3), descr="<i8"),
-            "ap_retained_canonical_ids.npy",
-            record,
-            [3, 3],
-        )
-        for payload in (
-            fixture_npy((3, 3), descr="<f8"),
-            fixture_npy((9,), descr="<i8"),
-        ):
-            with self.assertRaisesRegex(ValueError, "dtype/shape"):
-                sequence._ap_numpy_sidecar_contract(
-                    payload,
-                    "ap_retained_canonical_ids.npy",
-                    record,
-                    [3, 3],
+        sequence._identity_correction_payload_contract(payload, record, "fixture")
+        for forged in (bytes([0b00000101, 27, 12]), bytes([0b00000101, 13, 12])):
+            changed = dict(record)
+            changed["sha256"] = hashlib.sha256(forged).hexdigest()
+            with self.assertRaisesRegex(ValueError, "invalid base-3"):
+                sequence._identity_correction_payload_contract(
+                    forged, changed, "fixture"
                 )
-        float_shape = dict(record)
-        float_shape["shape"] = [3.0, 3.0]
-        with self.assertRaisesRegex(ValueError, "dtype/shape"):
-            sequence._ap_numpy_sidecar_contract(
-                fixture_npy((3, 3), descr="<i8"),
-                "ap_retained_canonical_ids.npy",
-                float_shape,
-                [3, 3],
-            )
 
     def test_json_integer_helpers_reject_integral_floats(self):
         self.assertEqual(sequence._positive_int(3, "positive"), 3)
@@ -2152,15 +2148,20 @@ class H007SequenceContainerTest(unittest.TestCase):
             "sha256": "a" * 64,
             "bitorder": "little",
         }
-        numpy_sidecar = {
-            "path": "ap_retained_canonical_ids.npy",
-            "shape": [1, 3],
-            "dtype": "<i8",
+        identity_corrections = {
+            "schema": "h007.ap_identity_corrections.v1",
+            "path": "ap_identity_corrections.bin",
+            "row_count": 1,
+            "mismatch_count": 0,
+            "mask_bytes": 1,
             "bytes": 1,
             "sha256": "a" * 64,
+            "bitorder": "little",
+            "base": "round-decoded-anchor-div-voxel-size",
+            "code": "uint8-base3-dx-dy-dz-plus1",
         }
         meta["__ap__"] = {
-            "schema": "h007.ap_gifstream.codec.v4",
+            "schema": "h007.ap_gifstream.codec.v5",
             "variant": {"name": "ap-gifstream-full"},
             "score": {"score_artifact_sha256": "a" * 64},
             "allocation": {},
@@ -2172,10 +2173,7 @@ class H007SequenceContainerTest(unittest.TestCase):
             "real_row_mask": {**mask, "path": "ap_real_row_mask.bin"},
             "padding_row_mask": {**mask, "path": "ap_padding_row_mask.bin"},
             "active_row_mask": {**mask, "path": "ap_active_row_mask.bin"},
-            "retained_canonical_ids": {
-                **numpy_sidecar,
-                "path": "ap_retained_canonical_ids.npy",
-            },
+            "identity_corrections": identity_corrections,
         }
         with self.assertRaisesRegex(ValueError, "unsafe/noncanonical|fields are unexpected"):
             sequence._required_gifstream_streams(meta, "ap-gifstream-full")
@@ -2200,7 +2198,7 @@ class H007SequenceContainerTest(unittest.TestCase):
         streams = sequence._required_gifstream_streams(
             meta, "ap-gifstream-full"
         )
-        self.assertIn("ap_retained_canonical_ids.npy", streams)
+        self.assertIn("ap_identity_corrections.bin", streams)
 
         meta["__ap__"]["score"]["anchor_count"] = 2
         meta["__ap__"]["score"]["eligible_count"] = 2
